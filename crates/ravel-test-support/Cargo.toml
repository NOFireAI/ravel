[package]
name = "ravel-test-support"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false
description = "Development-only test scaffolding shared across workspace crates: a parking clock and a runtime watchdog (ADR-1702)."

[dependencies]
parking_lot = { workspace = true }

[lints]
workspace = true
