//! ravel-server: gateway + ingest + query in one binary for development
//! (`--mode all|gateway|query`). Crate boundaries keep the split honest.

fn main() {
    println!("ravel-server: not yet implemented (Phase 1 in progress)");
}
