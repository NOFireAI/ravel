# Traceability: catalog MVCC invariants to their Rust source

Each row maps a TLA+ action or named property in `MCCatalogMVCC.tla` /
`CatalogMVCC.tla` to the catalog behavior it pins, to the Rust symbol that
implements that behavior, and to the test that exercises it (ADR-1113 D8).
`scripts/check-tla.sh traceability -a catalog` resolves the Rust references: a
reference is `crates/<path>.rs::Sym1::Sym2...`, and the check requires the file
to exist and every `::`-separated symbol to appear in it. A reference to a
`.tla` file is rejected: a traceability row cites the implementation, not the
model. The "new test needed" column names a gap where no test pins the modeled
clause yet, or `none` when an existing test covers it.

| TLA+ action or property | Meaning | Rust path and symbol | Existing test | New test needed |
|---|---|---|---|---|
| DoCommit | An L0 commit record is published into a not-yet-fold-sealed hour; a lost-response retry adopts the winner rather than duplicating | crates/ravel-commit/src/publish.rs::publish::put_data_object | crates/ravel-commit/src/publish.rs::put_data_object_is_idempotent_on_already_exists | none |
| DoCompact / CompactionPreservesMultiset | A compaction publishes only when the sum of sample_count over its inputs equals the sum over its outputs; the runtime gate is counts-only | crates/ravel-maintain/src/publish.rs::publish_record_with_conservation::conserve_exact | crates/ravel-maintain/src/publish.rs::conservation_mismatch_aborts_publish | none |
| DoCompactLoser / CompactionRecordImmutable | A compactor that loses the CreateIfAbsent race reads the winner's record back: it adopts the record when its own recomputed input set converges with the winner's, or reports the typed InputSetHashDivergence error when it does not, but never overwrites the already-published immutable record with its own recomputation either way | crates/ravel-maintain/src/publish.rs::resolve_already_exists | crates/ravel-maintain/tests/tombstone_race.rs::all_parts_already_exists_retry_retains_nothing_and_publishes and crates/ravel-maintain/tests/crash_matrix.rs::row11_already_exists_different_hash_alarms | none |
| CompactionPreservesMultiset (offline oracle) | The multiset-level equality the counts-only gate cannot catch is discharged end to end, not in this model | crates/ravel-query/tests/differential_compaction.rs::mixed_l0_l1_snapshot_end_to_end | crates/ravel-query/tests/differential_compaction.rs::dedup | none |
| DoFoldStart / seal predicates | A fold starts at the sealed watermark hour, which adds the fold safety margin on top of the maintenance seal so a bucket is compactable before it is foldable | crates/ravel-catalog/src/fold.rs::sealed_watermark_hour | crates/ravel-catalog/src/fold.rs::seal_boundary_is_inclusive_and_hour_by_hour | none |
| DoFoldPutPart / HeadNamesOnlyCompleteParts | The content-addressed snapshot part is written before HEAD names it | crates/ravel-catalog/src/fold.rs::part_object_key | crates/ravel-catalog/src/fold.rs::first_fold_rebuilds_from_commit_layout_and_second_call_is_idempotent | none |
| DoFoldCas / NoLiveCommitOmittedByLostCas | The version-matched HEAD CAS serializes racing folders; the winner omits no live commit at or below its watermark | crates/ravel-catalog/src/fold.rs::get_head::MAX_HEAD_CAS_ATTEMPTS | crates/ravel-catalog/src/fold.rs::reconcile_preserves_single_head_cas | none |
| DoRivalFoldWin / NoLiveCommitOmittedByLostCas | A concurrent catalog process wins the version-matched CAS and advances the watermark, bumping the object version; the modeled folder then finds its base version stale, loses its own CAS, and rebases rather than regressing HEAD | crates/ravel-catalog/src/fold.rs::get_head::MAX_HEAD_CAS_ATTEMPTS | crates/ravel-catalog/src/fold.rs::two_concurrent_first_folds_race_head_cas_and_only_one_advances | none |
| DoFoldCas fold-lifetime bound / DoFoldRebase | A fold commits within the protection horizon of the state it listed at, so a later compaction cannot have its inputs swept before the fold's CAS lands; a fold that outran the horizon rebases. Catalog::fold reads no clock; the bound the model calls fold-lifetime is enforced in code by the rebase taken when the HEAD CAS meets AlreadyExists or PreconditionFailed | crates/ravel-catalog/src/fold.rs::reconcile_one_bucket | crates/ravel-catalog/src/fold.rs::reconcile_applies_late_compaction_before_horizon, crates/ravel-catalog/src/fold.rs::fold_that_lists_before_a_compaction_and_cas_after_the_sweep_horizon_does_not_name_a_swept_input | none |
| DoReconcileTick / ReconcileOnlyOnWatermarkAdvance | Reconcile runs inside a watermark-advancing fold, never off a plain tick | crates/ravel-catalog/src/fold.rs::reconcile_one_bucket | crates/ravel-catalog/src/fold.rs::reconcile_applies_late_compaction_before_horizon | none |
| DoSweepSuperseded / HeadNamedObjectNeverDeleted | The superseded-input sweep deletes an L0 input past the protection horizon but object-granularly holds any input a valid HEAD still names (ADR-0020 delete blocker) | crates/ravel-maintain/src/sweep.rs::sweep_superseded_impl | crates/ravel-maintain/tests/superseded_head_gate.rs::head_named_superseded_inputs_are_held_not_deleted | none |
| DoSweepSuperseded delete path | Once the hour is reconciled and HEAD no longer names the inputs, the sweep deletes exactly the superseded inputs | crates/ravel-maintain/src/sweep.rs::sweep_superseded_impl | crates/ravel-maintain/tests/superseded_head_gate.rs::reconciled_hour_lets_the_sweep_delete_exactly_the_superseded_inputs | none |
| DoSweepCatalogObjects / SweepablePart | The unreferenced-part sweep reads the HEAD reference and deletes an orphan part only when no HEAD names it | crates/ravel-maintain/src/sweep.rs::sweep_unreferenced_catalog_objects::read_head_reference | crates/ravel-maintain/tests/superseded_head_gate.rs::absent_head_clears_the_gate_and_the_pass_deletes_as_before | none |
| DoSweepSuperseded / DoSweepCatalogObjects corrupt-HEAD guard / CorruptHeadFailsClosedOnDeletePaths | A corrupt or unsupported HEAD fails the reachability read, so both delete passes are guarded shut and the witnessed delete never fires under a non-valid HEAD | crates/ravel-maintain/src/reachability.rs::SnapshotReachability::ensure_head | crates/ravel-maintain/tests/superseded_head_gate.rs::unreadable_head_blocks_the_whole_pass_fail_closed | none |
| DoSweepSuperseded covering-part guard / CorruptHeadFailsClosedOnDeletePaths | The superseded-input sweep also fails closed when a covering snapshot part HEAD names cannot be decoded, distinct from HEAD itself being unreadable; the witnessed delete never fires while the part is unreadable | crates/ravel-maintain/src/reachability.rs::SnapshotReachability::object_gate::ensure_part | crates/ravel-maintain/tests/superseded_head_gate.rs::an_unreadable_covering_part_blocks_the_superseded_sweep_fail_closed | none |
| DoSweepSuperseded entry-identity guard / CorruptHeadFailsClosedOnDeletePaths | The superseded-input sweep also fails closed when a decoded snapshot entry's identity fields do not fit the shape a fold writes, so object_gate cannot decide whether it names a delete candidate; the witnessed delete never fires while the entry is undecodable | crates/ravel-maintain/src/reachability.rs::snapshot_object | crates/ravel-maintain/tests/superseded_head_gate.rs::an_undecodable_covering_entry_blocks_the_superseded_sweep_fail_closed | none |
| DoSweepCatalogObjects corrupt-HEAD guard / CorruptHeadFailsClosedOnDeletePaths | The unreferenced-part sweep has only the HEAD-decode trigger (it reads read_head_reference, not object_gate, so it has no covering-part or entry-identity concept of its own); a corrupt or unsupported HEAD fails the whole pass closed | crates/ravel-maintain/src/sweep.rs::sweep_unreferenced_catalog_objects::read_head_reference | crates/ravel-maintain/tests/superseded_head_gate.rs::absent_head_clears_the_gate_and_the_pass_deletes_as_before | none |
| DoTombstone / TombstonedBucketContributesNothing | A tombstone retires a bucket; a later fold contributes nothing for its hour | crates/ravel-maintain/src/retention.rs::write_tombstone | crates/ravel-catalog/src/fold.rs::reconcile_applies_late_tombstone | none |
| DoFoldStart / DoRivalFoldWin frontierReconciled / TombstonedBucketContributesNothing | ADR-0020 retention-frontier reconcile: a fold also re-diffs hours it would otherwise carry forward verbatim, at or below the retirement frontier and bounded per fold, so a tombstone below the fixed reconcile window still clears from HEAD and unblocks the sweep gate | crates/ravel-catalog/src/fold.rs::Catalog::fold | crates/ravel-catalog/src/fold.rs::frontier_reconcile_applies_out_of_window_tombstone | none |
| DoQueryResolve / MissingIndexDegradesToListing | A valid HEAD pins the snapshot; any other HEAD state degrades to a listing rather than erroring | crates/ravel-query/src/engine.rs::QueryEngine::resolve_snapshot_with_retry | crates/ravel-query/src/engine.rs::distributed_snapshot_invalidation_reresolves_once | none |
| DoQueryRun / PinnedSnapshotStableWithinAttempt | A pinned entry whose object was swept forces one re-resolve; a second miss is terminal SnapshotInvalidated | crates/ravel-query/src/error.rs::QueryError::SnapshotInvalidated | crates/ravel-query/src/engine.rs::distributed_snapshot_invalidation_reresolves_once | none |
| DoQueryRun / QueryTerminates | The resolve-and-retry ladder is bounded: a pinned query completes or is invalidated after at most one re-resolve, never spinning | crates/ravel-query/src/engine.rs::QueryEngine::resolve_snapshot_with_retry | crates/ravel-query/src/engine.rs::distributed_snapshot_invalidation_reresolves_once | none |
| SnapshotEntriesBelowWatermark | No HEAD snapshot entry sits above the watermark it was folded at | crates/ravel-catalog/src/fold.rs::sealed_watermark_hour | crates/ravel-catalog/src/fold.rs::reconcile_ignores_late_record_outside_window | none |
| SignalDedupContract | Under metrics, query-time dedup by identity collapses a two-source conflict to one; under logs or spans no dedup runs | crates/ravel-query/src/engine.rs::is_greater | crates/ravel-query/tests/differential_compaction.rs::dedup | none |
| LateSupersessionEventuallyReflected | A published compaction record is eventually reflected in a valid HEAD: its inputs leave the snapshot and its L1 part enters it | crates/ravel-catalog/src/fold.rs::reconcile_one_bucket | crates/ravel-catalog/src/fold.rs::reconcile_applies_late_compaction_before_horizon | none |
