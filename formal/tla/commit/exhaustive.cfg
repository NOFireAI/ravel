\* Two-shard, two-content safety coverage: the smallest bounds that still
\* give cross-shard visibility, the marker and partial-reporting
\* invariants, and OneIdentityOneContent's identity-reuse case their
\* required interleavings. MaxRetries=0 here (not 1): at MaxRetries=1 this
\* cfg's state graph does not converge inside the harness's 3600-second
\* budget, so retry interleavings are covered instead by
\* negative/at-least-once-duplicate-reachable.cfg and dedup-mutant.cfg,
\* both at MaxRetries=1. See results.md for the non-completion figures.
\*
\* CheckQuery=FALSE and no NoUncommittedDataVisible invariant here, for the
\* same kind of reason as MaxRetries above: at MaxTicks=2 the RunQuery
\* action's single firing can land at enough distinct reachable states that
\* it alone stops this cfg from converging, and the invariant is already
\* covered at these same Shards/Contents bounds by smoke.cfg (MaxTicks=1)
\* plus the negative and mutant coverage in negative/query-reads-uncommitted-
\* data.cfg. See results.md for the before/after figures.
\*
\* CheckToken=FALSE and no TokenNeverServesStale invariant here, for the same
\* reason and by the same mechanism: ResolveToken (and the TombstoneBucket/
\* SupersedeRecord actions that feed it) fire once at an arbitrary reachable
\* state, and that timing choice is what kept this cfg from completing with
\* real margin under the harness's 3600-second budget even after CheckQuery
\* was disabled. The invariant is a per-flush property that does not depend
\* on shard count, so smoke.cfg (MaxTicks=1, CheckToken=TRUE) already proves
\* it exhaustively; this cfg keeps the two-shard coverage that IS specific to
\* it (the marker and partial-reporting invariants, and deadline
\* reachability) and gives up re-proving TokenNeverServesStale at these
\* bounds too. See results.md for the before/after figures.
CONSTANTS
    Writers = {w1}
    Shards = {s1, s2}
    Contents = {c1, c2}
    MaxRetries = 0
    FlushLifetime = 1
    MaxTicks = 2
    Signal = "logs"
    HasIdemKey = TRUE
    CommitBeforeData = FALSE
    SkipHashCompare = FALSE
    AckAtEnqueue = FALSE
    MarkerAfterFirstShard = FALSE
    RetryDedups = FALSE
    QueryReadsDataDirectly = FALSE
    CheckQuery = FALSE
    CheckToken = FALSE
CHECK_DEADLOCK FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoCommitWithoutData
INVARIANT StrictAckImpliesDurable
INVARIANT OneIdentityOneContent
INVARIANT SplitBrainStopsTheShard
INVARIANT RetrySamePinnedFlushIdempotent
INVARIANT NoPublishAfterAbandon
INVARIANT MarkerImpliesAllShardsDurable
INVARIANT PartialReportingMatchesSignal
